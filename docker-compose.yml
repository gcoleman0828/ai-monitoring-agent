version: "3.9"

services:
  ai-monitoring-api:
    build: .
    container_name: ai-monitoring-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
    working_dir: /app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
